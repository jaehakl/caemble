# A Caemble user AccessKey with caemble scope, issued from the web account page.
CAEMBLE_API_URL=https://caemble.example/api
CAEMBLE_API_TOKEN=
# Optional: otherwise resolved by poetry env info --executable in this checkout's CAE directory.
# CAEMBLE_PYTHON=/absolute/path/to/cae/.venv/bin/python
