/** Old shared links remain usable after sections are renamed or moved. */
export const documentationAnchorRedirects: Readonly<Record<string, Readonly<Record<string, { item: string; anchor: string }>>>> = {}
