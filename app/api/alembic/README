The API starts from the single destructive `000000000001` baseline. Existing
application schemas must be erased with `reset_schema.py` before applying it;
the baseline does not support downgrade recovery.
