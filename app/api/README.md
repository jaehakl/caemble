# Caemble API

FastAPI owns authentication, persistence, artifact uploads, and job orchestration.
See [API setup and development](../../docs/development/api.md) for PostgreSQL,
OAuth, migrations, and configuration.

After setup, run `poetry run uvicorn main:app --app-dir app --reload` here.
Deployment instructions are in [operations](../../docs/operations/deployment.md).
